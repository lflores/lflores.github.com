'use strict';

angular
    .module('myApp')
    .component('header', {
        templateUrl: 'components/header/header.html',
        controller: function ($scope, $element, $attrs) {

        }
    });
